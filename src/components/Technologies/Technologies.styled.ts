import { SectionBackground } from "@/styles/common.style";
import { Box } from "@mantine/core";
import styled from "styled-components";

export const TechnologiesContainer = styled(Box)`
  height: 800px;
  padding-top: 48px;
  width: 100%;
  color: #fff;
`;

export const SectionHeading = styled(Box)`
  font-size: 45px;
  font-weight: 600;
  text-align: center;
`;
